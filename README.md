# bio-data-tool
* for the HMDP data, i changed the name of the first column to 'Strains' since the name was empty originally
* this gui only really works for the HMDP data as well
* to run, do the folowing:
    * 'pip install -r requirements.txt'
    * 'python basic_python_gui.py'